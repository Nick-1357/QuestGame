import Popup from 'reactjs-popup'

/*
const Question = props => {
    return (<Popup trigger = {} >
        <p>{props.question}</p>
        <button> Generate hint</button>
        <button> Submit answer</button>
    </Popup>
    //trigger will be answering the question right
    );
}

export default Question;
*/